# Minimal task template

Copy `task.py`, then add a scene builder, controller, dataset JSON, scripted
controller YAML and SmolVLA YAML. Register the resulting `TASK_SPEC` in
`tasks/__init__.py`. Keep recorder/converter/trainer code unchanged.
